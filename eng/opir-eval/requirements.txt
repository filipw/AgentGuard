# Phase-0-light Opir (GLiClass) eval harness.
# CPU-only is fine; this is an offline measurement tool, not shipped code.
gliclass>=0.1.7
torch>=2.4
transformers>=4.44
tokenizers>=0.19
onnxruntime>=1.18
numpy>=1.26
sentencepiece>=0.2
datasets>=3.0  # multilingual toxicity sets (parquet; avoids the rate-limited rows API)
